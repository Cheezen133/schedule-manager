# 日程管理系统 — 开发环境说明书

## 一、运行环境

| 环境 | 最低版本 | 推荐版本 | 用途 |
|---|---|---|---|
| **Python** | 3.10 | 3.11-3.14 | 后端 API 服务 |
| **Node.js** | 16 | 18-22 | 前端构建 + 开发服务器 |
| **npm** | 8 | 9+ | 随 Node.js 安装，前端包管理 |
| **pip** | 21 | 24+ | 随 Python 安装，后端包管理 |
| **操作系统** | Windows 10+ / macOS 12+ / Linux | — | — |

> 不需要安装数据库。项目使用 SQLite，Python 自带支持。

---

## 二、Python 后端依赖

```txt
fastapi>=0.110.0          # Web 框架
uvicorn[standard]>=0.29.0 # ASGI 服务器（运行后端）
sqlalchemy>=2.0.0         # 数据库 ORM
python-jose[cryptography]>=3.3.0  # JWT 令牌签发与校验
pydantic>=2.0.0           # 请求/响应数据校验
pydantic[email]           # 邮箱校验（pydantic 扩展）
python-multipart>=0.0.9   # 文件上传解析
icalendar>=5.0.0          # iCal 日历导出
slowapi>=0.1.9            # API 限流
python-dotenv>=1.0.0      # .env 环境变量读取
```

**安装命令**：

```bash
cd backend
pip install -r requirements.txt
```

---

## 三、Node.js 前端依赖

```json
{
  "react": "^18.2.0",              // UI 框架
  "react-dom": "^18.2.0",          // React DOM 渲染
  "react-router-dom": "^6.20.0",   // 前端路由
  "@fullcalendar/react": "^6.1",   // 日历组件
  "@fullcalendar/daygrid": "^6.1", // 月视图
  "@fullcalendar/timegrid": "^6.1",// 周/日视图
  "@fullcalendar/interaction": "^6.1", // 点击交互
  "axios": "^1.6.0",              // HTTP 请求库
  "vite": "^5.0.0",               // 构建工具
  "@vitejs/plugin-react": "^4.2.0" // React 编译插件
}
```

**安装命令**：

```bash
cd frontend
npm install
```

---

## 四、项目目录结构

```
schedule-manager/
├── backend/                 # Python 后端
│   ├── app/
│   │   ├── main.py          # FastAPI 入口（启动文件）
│   │   ├── config.py        # 配置文件（数据库、JWT 密钥等）
│   │   ├── database.py      # SQLAlchemy 数据库引擎
│   │   ├── dependencies.py  # 认证/角色依赖注入
│   │   ├── models/          # ORM 数据模型
│   │   │   ├── user.py      #   用户表
│   │   │   ├── schedule.py  #   日程表（核心）
│   │   │   ├── category.py  #   分类表
│   │   │   ├── attachment.py#   附件表
│   │   │   ├── message.py   #   沟通消息表
│   │   │   └── verification.py # 验证码 + 审计日志表
│   │   ├── schemas/         # Pydantic 请求/响应模型
│   │   ├── routers/         # API 路由（控制器）
│   │   │   ├── auth.py      #   登录认证
│   │   │   ├── schedules.py #   日程 CRUD + 审核
│   │   │   ├── categories.py#   分类管理
│   │   │   ├── attachments.py#  附件上传下载
│   │   │   ├── messages.py  #   客户沟通消息
│   │   │   └── export.py    #   iCal 导出
│   │   ├── services/        # 业务逻辑层
│   │   └── utils/           # 工具函数（JWT 编解码）
│   ├── scripts/
│   │   └── promote_user.py  # 命令行：提升用户角色
│   └── requirements.txt     # Python 依赖清单
│
├── frontend/                # React 前端
│   ├── src/
│   │   ├── main.jsx         # 应用入口
│   │   ├── App.jsx          # 路由配置
│   │   ├── api/             # API 调用封装（axios）
│   │   ├── contexts/        # React Context（认证状态）
│   │   ├── pages/           # 页面组件
│   │   │   ├── LoginPage.jsx         # 登录
│   │   │   ├── CalendarPage.jsx      # 日历主视图
│   │   │   ├── CreateSchedulePage.jsx# 新建日程
│   │   │   ├── EditSchedulePage.jsx  # 编辑日程
│   │   │   ├── ScheduleDetailPage.jsx# 日程详情
│   │   │   ├── ReviewPage.jsx        # 审核队列
│   │   │   ├── CategoriesPage.jsx    # 分类管理
│   │   │   └── ContactsPage.jsx      # 联系人
│   │   ├── components/      # 可复用组件
│   │   │   ├── layout/      #   AppLayout, Header, Sidebar
│   │   │   ├── auth/        #   手机号输入，验证码输入
│   │   │   ├── schedule/    #   日程表单、卡片、审核栏、附件面板
│   │   │   ├── calendar/    #   FullCalendar 封装
│   │   │   └── common/      #   路由守卫、加载动画、语音输入按钮
│   │   └── styles/          # 全局 CSS
│   ├── dist/                # 构建产物（生产用）
│   ├── index.html           # HTML 模板
│   ├── package.json         # Node 依赖清单
│   └── vite.config.js       # Vite 配置（含 API 代理）
│
├── .env                     # 环境变量
├── .gitignore               # Git 忽略规则
├── start.bat                # 一键启动脚本（Windows）
├── README.md                # 项目说明
└── 环境说明.md               # 本文件
```

---

## 五、启动方式

### 方式一：一键启动（Windows）

```bash
双击 start.bat
```

会自动安装依赖并启动前后端。

### 方式二：手动启动

**终端 1 — 启动后端（端口 8000）：**

```bash
cd schedule-manager/backend
pip install -r requirements.txt    # 首次运行
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

**终端 2 — 启动前端（端口 5173）：**

```bash
cd schedule-manager/frontend
npm install                        # 首次运行
npm run dev
```

浏览器访问 `http://localhost:5173`

---

## 六、API 文档

后端运行后，浏览器访问 `http://localhost:8000/docs` 查看 Swagger 交互式 API 文档。

---

## 七、数据库

- **引擎**：SQLite（文件型数据库）
- **文件**：`backend/schedule_manager.db`（首次启动自动创建）
- **无需安装**：Python sqlite3 模块内置支持
- **切换生产数据库**：修改 `.env` 中的 `DATABASE_URL` 为 PostgreSQL/MySQL 连接串

---

## 八、环境变量

编辑 `schedule-manager/.env`：

| 变量 | 默认值 | 说明 |
|---|---|---|
| `DATABASE_URL` | `sqlite:///./schedule_manager.db` | 数据库连接 |
| `JWT_SECRET_KEY` | (开发用默认值) | **生产环境必须更换** |
| `JWT_EXPIRE_HOURS` | `24` | JWT 过期时间 |
| `DEV_MODE` | `true` | 开发模式（验证码固定 123456） |
| `CORS_ORIGINS` | `http://localhost:5173` | 允许的前端域名 |

---

## 九、三方服务依赖

无需任何第三方服务即可运行。后续可选接入：

| 服务 | 用途 | 状态 |
|---|---|---|
| 短信平台（阿里云/腾讯云） | 真实验证码发送 | 待接入，改 `auth_service.py` |
| 微信公众号/企业微信 | 微信消息互通 API | 需企业认证，暂无 |

---

## 十、角色说明

| 角色 | 英文 | 权限 |
|---|---|---|
| 管理员 | `admin` | 所有权限：分类管理、修改任意日程、审核、设置可见性 |
| 阅读者 | `reader` | 审核批准/驳回、查看日程 |
| 录入者 | `writer` | 创建日程、编辑/删除自己的日程 |

首次登录默认为 `writer`，使用以下命令提升：

```bash
cd backend
python scripts/promote_user.py --phone 手机号 --role admin
```

---

## 十一、生产部署要点

1. 修改 `.env` 中 `JWT_SECRET_KEY` 为 64 位以上随机字符串
2. 设置 `DEV_MODE=false`
3. 前端构建：`cd frontend && npm run build`
4. 后端直接托管前端静态文件（代码已内置，自动检测 `frontend/dist/`）
5. 单端口部署：`python -m uvicorn app.main:app --host 0.0.0.0 --port 80`
6. 建议使用 Nginx 反向代理 + HTTPS
