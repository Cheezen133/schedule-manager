# 病历审阅板块（含每日汇报）

> 本工作流在根目录 `AGENTS.md` 的 ROUTES 表中对应「开发「病历审阅」板块（含每日汇报）前」那一行。

## 已定决策（2026-09-25 用户确认）

- PDF 直接上传，不脱敏；数据存原作者的本地私人服务器。全部代码最终交给原作者。
- PDF 不设容量上限：后端按块写盘，不把整份文件读进内存。
- 板块是通用的，结论只有三项：是否纳入（纳入／不纳入／待定）、病因诊断（文字）、审阅意见（文字），不带任何课题专属选项。
- 审阅人账号在原作者系统里已有，本仓库不建；开发测试只用临时库里的测试账号和假病历。
- 板块下有「每日汇报」子区：按日期排成时间轴，每条可带文字、图片和文件。

## 设计

**组织方式**：板块里先建「项目」，项目有成员。病历和每日汇报都属于某个项目，只有项目成员和管理员能看。项目创建者（或管理员）管理成员。

**病历**：成员按编号建病历，可上传多份 PDF，指派一名成员当审阅人。审阅人提交结论；状态由结论推出：未指派／待审阅／待定／已纳入／未纳入。结论按「病历 × 审阅人」各存一份，以后加二次审阅不用改表。

**批注**：不写进 PDF，单独存（文件、页码、类型、位置、内容、作者）。类型有点注和框选两种，位置按页面宽高的比例存（0–1），手机和电脑上都能对上。任何成员都能加批注，只有作者本人能改或删。

**PDF 阅读**：前端用 pdfjs-dist 的 legacy 构建（兼容较旧的企业微信内置浏览器），关闭 `isEvalSupported`；文件经登录校验的接口取回后交给 PDF.js 渲染，只渲染视口附近的页面。

**每日汇报**：成员发帖，填日期（默认今天）、文字、若干图片或文件；按日期倒序排成时间轴。作者本人能改或删。

**导出**：项目内一键导出 CSV（UTF-8 带 BOM，Excel 可直接打开），列为编号、标题、审阅人、结论、病因诊断、审阅意见、审阅时间。

**日志**：查看或下载 PDF、保存结论、删除病历或文件时，写入 `review_logs`。

**权限**（照 `LOGIC_BUG_AUDIT.md` 的教训，每个接口都校验，文件下载也不例外）：

| 操作 | 谁可以 |
|---|---|
| 看项目、病历、文件、批注、结论、汇报，导出 | 项目成员、管理员 |
| 改或删项目，管理成员 | 项目创建者、管理员 |
| 建病历、上传文件、加批注、发汇报 | 项目成员 |
| 改或删病历、删文件 | 病历创建者（文件则是上传者）、项目创建者、管理员 |
| 提交或修改结论 | 该病历被指派的审阅人 |
| 改或删批注、汇报 | 作者本人、管理员 |

**后端**：模型放在 `backend/app/models/case_review.py`，接口放在 `backend/app/routers/case_review.py`（前缀 `/api/v1/case-review`），返回格式沿用 `{"code": 0, "data": ...}`。新表在 `models/__init__.py` 注册后，由 `migrate_database` 最后一步的 `create_all` 建出。

**前端**：
- 路由：`/case-review`（项目列表）、`/case-review/:projectId`（项目页，分「病历／每日汇报／成员」三栏）、`/case-review/:projectId/cases/:caseId`（病历页：左边 PDF，右边批注和结论；手机上 PDF 全屏，批注和结论从底部弹出）。
- 入口：电脑放侧边栏，手机放「我的」。
- 基础样式放 `styles/caseReview.css`，手机样式按惯例写进 `styles/mobile.css`。

## 第一期步骤

- [x] 1. 后端模型、注册、建表 —— 验证：临时库 `create_all` 后 `schema_differences()` 为空；OpenAPI 中有 19 条 `/api/v1/case-review` 路径
- [x] 2. 后端接口（项目与成员、病历与文件、批注、结论、每日汇报、导出、日志）—— 验证：`backend/tests/test_case_review.py` 9 个测试加原有 9 个全部通过
- [x] 3. 前端 API 封装、路由、电脑和手机入口 —— 验证：侧边栏与「我的」入口可达，待审阅角标随指派和结论变化
- [x] 4. 项目列表与项目页（含成员管理）—— 验证：340px 与 1280px 各走一遍；搜索并添加成员、移除成员（确认框）均正常
- [x] 5. PDF 阅读、点注和框选批注、结论面板 —— 验证：英文、中文（不嵌入宋体，靠 cmaps）、纯图片扫描件三种假 PDF 都能显示；点注、框选、批注列表跳转、修改删除、翻页、缩放后停在原页、远处页面释放画布、关闭阅读器走浏览器后退；审阅人提交纳入与待定，状态与角标随之更新
- [x] 6. 每日汇报时间轴（文字、图片、文件）—— 验证：发、改、删（确认框）正常，图片缩略图与全屏查看正常；文件下载只核对了接口，未在界面点下载
- [x] 7. 导出 CSV —— 验证：带 BOM、表头与各行内容和页面一致、编号按 1、2、10 排序（未在 Excel 里实际打开）
- [x] 8. 回归 —— 验证：后端 18 个测试通过；`vite build` 通过；日历页在两种宽度下不受影响

未验证：企业微信内置浏览器与 iOS Safari 真机（只在 Chromium 预览里测过）；上百页或几十 MB 的大病历在手机上的速度；双指缩放（依赖浏览器自带缩放）。

- [ ] 交付提醒：新表不会在启动时自动建，缺表时后端直接拒绝启动。原作者部署前先运行 `cd backend && python -m scripts.migrate_database`（MySQL 上可用；脚本里有 MySQL 专用写法，SQLite 上建新表用 `Base.metadata.create_all`）

## 第二期（待第一期完成后再定）

批注回复、消息通知、二次审阅、导出带批注的 PDF、按文件名批量建病历。

## Review

（全部完成后填写：结果、遗留问题、教训的去向。）
