# 项目逻辑审查与后续修改基线

审查日期：2026-07-21  
范围：`backend/app` 与 `frontend/src` 的业务代码静态审查。本文只记录问题和修改建议，**本次没有修改业务逻辑**。

## 项目业务边界

- 角色：`admin`、`reader`、`writer`。
- 核心对象：日程、日程审核、日程码委托管理、日程附件/客户沟通、独立聊天与共享文件。
- 关键原则：同一份日程、附件、沟通记录、聊天媒体都必须以“当前用户是否有访问该资源的权限”为准，而不应仅凭资源 ID 存在与否。

## 已确认的高优先级问题

| ID | 位置 | 问题与影响 | 建议修复方向 |
| --- | --- | --- | --- |
| P0-01 | `backend/app/routers/attachments.py:45-190` | 上传、列表和下载仅验证日程/附件存在，未验证当前用户是否是日程创建者、管理员或日程码被授权管理者。任意已登录 writer 可向任意日程写入附件；任意已登录用户可枚举 ID 读取附件及文件内容。`include_history` 还会按联系人电话跨日程返回资料。 | 抽取统一的 `can_access_schedule`/`can_modify_schedule` 校验，在上传、列表、下载均执行；历史查询仅限当前用户可访问的日程集合。 |
| P0-02 | `backend/app/routers/messages.py:46-190` | 客户沟通记录读取、发文字、发语音均没有日程权限校验；语音播放接口也完全不要求登录。已登录用户可读取或伪造其他日程的沟通记录，知道文件名时甚至可匿名读取语音。 | 复用日程访问校验；播放接口增加认证并由消息记录反查授权；历史查询先过滤可访问日程。 |
| P0-03 | `backend/app/routers/chat.py:374-388, 487-493, 579-596, 652-662` | 聊天媒体查看/下载接口没有认证；共享文件列表没有检查会话参与者；删除共享文件没有检查上传者、会话参与者或管理员。任何人只要猜到文件名、会话 ID 或共享文件 ID，就可能下载、枚举或删除他人资料。 | 媒体/下载接口通过文件名反查 `ChatMessage` 或 `SharedFile`，再验证会话成员；共享文件的列表/删除也必须验证会话成员，删除额外限制上传者或管理员。 |
| P1-01 | `backend/app/routers/schedules.py:133-159`、`backend/app/services/schedule_service.py:109-130` | 创建时校验了 `end_time > start_time`，但编辑时没有。在仅修改开始或结束时间的 API 调用中可保存结束早于开始的无效日程，破坏日历展示和 iCal 导出。 | 合并“现有值 + 请求中变更值”后统一校验时间区间；前端校验仅作体验优化，后端必须兜底。 |
| P1-02 | `backend/app/services/schedule_service.py:109-130` | 日程所有者编辑已确认日程会重置为待审核，但编辑被驳回日程后仍保持 `rejected`，无法重新进入审核队列；前端没有单独的重新提交动作。 | 明确状态机。通常所有者对 `confirmed` 或 `rejected` 的内容修改都应转为 `pending`，并清空旧审核字段。 |
| P1-03 | `backend/app/services/schedule_service.py:128, 304-322` | 审计日志的 `performed_by` 在编辑、完成切换、完成人修改时使用 `schedule.created_by`，而非实际执行者。管理员或日程码管理者代操作后，审计会错误归因给创建者。 | 把实际操作者 ID 传入服务层，并在每一个 `_log_action` 调用中写入该 ID。 |
| P1-04 | `backend/app/routers/dashboard.py:75-84` | 非管理员仪表盘的分类分布查询没有套用 `base_filter`，因此统计会包含其他用户日程；分类名称和数量会泄露跨用户业务活动。 | 在分类联表查询也应用与其他统计相同的可见范围过滤。 |
| P1-05 | `backend/app/routers/chat.py:419-440, 459-484` | 收藏消息时未验证当前用户属于该消息所在会话。用户可收藏任意已知消息 ID，并在“我的收藏”中取得消息内容及对方信息。 | 收藏前先读取会话并校验成员；历史收藏在读取时也应跳过已无权访问的数据。 |

## 中优先级问题

| ID | 位置 | 问题与影响 | 建议修复方向 |
| --- | --- | --- | --- |
| P2-01 | `backend/app/schemas/schedule.py:26-41`、`backend/app/services/schedule_service.py:109-130` | 通用编辑请求允许直接写入 `is_completed`，但不会同步 `completed_at`；而专用完成接口会同步两者。数据因此可能出现“已完成但没有完成时间”或反向不一致。 | 将完成状态从通用更新字段中移除，或在服务层集中维护 `is_completed` 与 `completed_at` 的一致性。 |
| P2-02 | `backend/app/services/schedule_service.py:14-46, 109-130` | `category_id` 在创建和编辑时没有严格验证分类存在且可用。SQLite 未开启外键时可留下悬挂分类 ID；MySQL 则可能在提交时得到不友好的完整性错误。 | 在创建/更新前查询分类，缺失或停用时返回明确的 400/404。 |
| P2-03 | `backend/app/routers/users.py:153-214` | 注销用户仅删除该用户发送的聊天消息和会话行，没有清理相关的共享文件、收藏、好友请求、聊天媒体物理文件，且依赖数据库是否真正启用级联删除。MySQL 环境可能被外键阻断，SQLite 环境则可能遗留孤儿记录/文件。 | 设计并按顺序执行完整的删除事务：好友请求、收藏、共享文件、聊天消息与其物理文件、会话、日程关联数据；失败时回滚。 |
| P2-04 | `backend/app/services/schedule_code_service.py:18-29` | 随机日程码在连续 20 次碰撞后仍会使用循环最后生成的 `code` 创建记录，未显式失败；并发创建时也没有处理唯一约束冲突。 | 循环结束后若仍冲突则抛出可处理异常；提交时捕获唯一约束冲突并重试有限次数。 |
| P2-05 | `backend/app/routers/chat.py:520-548` | 接受好友请求时未检查其当前状态是否仍是 `pending`，重复请求或并发重复提交可能创建多个相同参与者的会话；模型也没有 `(user1_id, user2_id)` 唯一约束。 | 仅接受 `pending` 请求；为排序后的用户对建立唯一约束，并在冲突时返回已有会话。 |

## 修改顺序建议

1. 先处理 P0：统一资源授权校验，并为附件、客户沟通、聊天媒体和共享文件补回归测试。
2. 再处理日程状态与时间完整性（P1-01、P1-02、P2-01），把状态转移集中到服务层。
3. 修正审计归因和仪表盘数据范围（P1-03、P1-04）。
4. 最后补足删除级联、分类校验和并发唯一性保护（P2）。

## 已落实的变更（2026-07-21）

- 日程码管理者或管理员代为编辑日程时，系统会将日程设为 `pending`，并标记为“需日程拥有者审核”。
- 该类待审核日程仅创建者可批准或驳回；批准后才转为 `confirmed`。
- 日历中的 `confirmed` 日程固定采用绿色显示，不再被分类颜色覆盖。
- 聊天页打开后立即加载好友请求并每 10 秒刷新，收到好友申请时无需先点击图标即可显示红点。
- 附件、客户沟通语音和聊天媒体/共享文件现均在读取、上传或删除前校验日程或会话访问权限，避免通过 ID 或文件名越权访问。
- 聊天现支持文字消息右键收藏与加入共享资料、直接粘贴图片发送，以及自定义名称的群聊；群聊与私聊使用同一份成员权限和共享文件机制。
- 文字消息右键可直接跳转至新建日程页面，并将消息内容预填为日程标题。

## 建议新增的回归测试

- 未授权用户请求他人日程的附件、消息、语音、聊天媒体、共享文件：均返回 403 或 404，且不会暴露文件内容。
- 所有者把被驳回日程修改后：状态变为 `pending`，可再次进入审核列表。
- 分别只修改 `start_time`、只修改 `end_time` 为非法区间：API 返回 400，数据库不变。
- 管理员/日程码管理者编辑或完成日程：审计日志中的操作者为实际用户。
- 非管理员请求仪表盘：分类统计只包含自己的日程。
- 重复接受同一好友请求：只保留一个会话；注销含聊天附件的账号：事务成功且数据库、磁盘均无孤儿数据。

## 本次验证说明

- 已完成完整目录与主要前后端调用链的静态审查。
- 前端依赖目录存在，但在当前沙箱中执行 `npm run build` 时，`esbuild` 因无法读取上级目录而失败；该失败属于当前执行环境限制，不能据此判定项目构建配置有问题。
- 当前环境未提供可直接调用的 `python` 命令，因此本次未运行后端集成测试。后续具体修改时应在项目实际 Python 运行环境补充并执行测试。
